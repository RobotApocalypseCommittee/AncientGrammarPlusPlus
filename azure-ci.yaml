steps:
# CMake
# Build with the CMake cross-platform build system
- task: CMake@1
  inputs:
    workingDirectory: 'build'
    cmakeArgs: ".."
-bash: "cd build; ls;"
- task: CMake@1
  inputs:
    workingDirectory: 'build'
    cmakeArgs: "--build .. --target all --"
